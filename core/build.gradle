plugins {
    id 'java'
    id 'maven-publish'
}

sourceSets.main.java.srcDirs = ["src"]

publishing {
    publications {
        maven(MavenPublication) {
            groupId = "$groupName"+"." + rootProject.name
            artifactId = 'core'//你的模块名称
            version = "$Version"
            from components.java
        }
    }
}

dependencies {
    repositories{
        mavenCentral()
        maven{ url 'https://www.jitpack.io' }
    }
    //这里写你需要的依赖，但是局部
}
